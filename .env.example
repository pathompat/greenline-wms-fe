# Base URL of the WMS backend API.
# Baked in at BUILD time by Vite — changing it on Render requires a rebuild/redeploy.
# Must be https in production, otherwise the browser blocks it as mixed content.
VITE_API_URL=https://dev-app.greenlinepetcare.co.th
